# Atualização segura — Google Places

Esta pasta foi gerada sobre o ZIP atual do Achilles Command enviado pelo usuário.

Foram preservados de propósito:

- `config.js` existente;
- `.gitignore` existente;
- `supabase/seed.sql` existente;
- diretório `.git` e histórico local.

## O que fazer agora

1. Abra `docs/GUIA_IMPLEMENTACAO.md` e siga a Parte 7.
2. Execute `supabase/migration_2026_08_03_google_places.sql` no SQL Editor.
3. Configure `GOOGLE_PLACES_API_KEY` no Netlify.
4. Faça novo deploy.
5. Teste `Academia / Patrocínio / MG`.

Depois da validação da captação, siga para Claude e WhatsApp.
